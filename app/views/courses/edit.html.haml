- content_for :title do
  Ertex47Project
  \|
  Редактирование курса
  =@course.title

= link_to 'Курсы', courses_path

.fa.fa-graduation-cap
= link_to course_path(@course) do
  = @course.title

= link_to edit_course_path(@course) do
  Редактирование

.card
  .card-header
    %h3
      .fa.fa-graduation-cap
      Редактирование курса
  .card-body  
    = render 'form'
  .card-footer
    = link_to 'Отмена', @course
